---
'@drivenets/design-system': patch
---

Give the colliding `Components/Table/Filters` story files unique titles (`Filters`, `Search`, `Filters Panel`) so every file reaches the MCP component manifest, and keep the full-toolbar showcase stories out of the manifest
