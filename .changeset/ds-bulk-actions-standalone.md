---
'@drivenets/design-system': minor
---

Add `DsBulkActions` standalone component with discriminated action items (`icon`/`label` or custom `render`), `placement` (`static` | `floating`), and table integration via `placement="floating"`.
